name: Deploy to GitHub Pages

on:
  push:
    # claude/... is the repo's current default branch; main is included so the
    # workflow keeps working if/when a main branch is created and made default.
    branches:
      - claude/sprint-transitions-site-v4nm8l
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Project pages are served from /<repo-name>/, so set Vite's base
      # accordingly. Local dev and builds keep the default base of /.
      - run: npm run build -- --base=/${{ github.event.repository.name }}/

      - uses: actions/configure-pages@v5
        with:
          # Enables GitHub Pages (source: GitHub Actions) on first run if the
          # repo doesn't have it turned on yet.
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
